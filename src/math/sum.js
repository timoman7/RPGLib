/**
 * @module Math
 * @submodule Sum
 * @for RPGLib
 * @requires core
 */

'use strict';

var RPGLib = require('../core/core');

RPGLib.sum = function(a,b){
  return a+b;
};

module.exports = RPGLib;
